# 1. What does "touch" command do?

# a.
# Task: Create an empty file named todo.txt.

# b.
# Task: Create three files named index.html, style.css, and script.js in one command.

# c.
# Task: 
# Create a file called server.log.
# Update the timestamp of the existing file called server.log without modifying its content.

# d.
# Task:
# Create a directory called projects.
# Create a file named notes.md inside the directory called projects.
# (Assume the directory already exists.)

# 2. rm
# i. What does "rm" command stands for?
# ii. Why do we use it?

# a.
# Task: 
# Create a file named old_notes.txt.
# Delete the file named old_notes.txt.

# b.
# Task: 
# Create three files named a.txt, b.txt, and c.txt using one command.
# Delete the three files named a.txt, b.txt, and c.txt using one command.

# c.
# Task:
# Create a file named config.json.
# Delete the file named config.json, but make sure Bash asks for confirmation first.

# d.
# Task: 
# Create a directory named temp, then create file1.txt and file2.txt inside it.
# Delete the directory named temp, including all files inside it, with confirmation prompts.

# 3. rmdir
# i. What does "rmdir" stand for?
# ii. Why do we use it?

# a.
# Task:
# Create a directory called temp.
# Remove the empty directory named temp.

# b.
# Task:
# Create three directories called jan, feb, and mar with one command.
# Remove the three empty directories named jan, feb, and mar in one command.

# c.
# Task:
# Create a nested directory projects/app/src.
# Remove the nested directory projects/app/src, and also remove parent directories if they become empty.

# d.
# Task: You try to remove a directory named downloads, but it contains files.
# What command will fail, and why?

# 4. cp
# i. What does "cp" stands for?
# ii. Why do we use it?

# a.
# Task:
# Create a file named data.txt and write "Hello, data!" inside it.
# Copy the file named data.txt to a new file called data_backup.txt.

# b.
# Task: 
# Create a file called photo.jpg
# Copy the file named photo.jpg into a directory called images.
# (Assume the directory already exists.)

# c.
# Task: 
# Create a directory called scripts, then create three files, a.js, b.js, c.js
# Copy the three files — a.js, b.js, and c.js — into the directory named scripts.

# d.
# Task:
# Create a directory named project, then move to it.
# When inside project directory, create a directory named requirements and a file called webpack.json.
# Copy the directory named project and all of its contents into a new directory called project_backup.
